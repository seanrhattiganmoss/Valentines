<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>For Joy</title>
    <link href="https://fonts.googleapis.com/css2?family=Dancing+Script:wght@700&family=Quicksand:wght@400;500&display=swap" rel="stylesheet">
    <style>
        body {
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            font-family: 'Quicksand', sans-serif;
            text-align: center;
            overflow: hidden;
            /* Lilies and Hearts background styling */
            background-color: #fff5f7;
            background-image: 
                url('https://www.transparenttextures.com/patterns/pinstripe.png'),
                radial-gradient(circle, rgba(255,255,255,0.8) 0%, rgba(255,240,245,0.5) 100%);
            position: relative;
        }

        /* Decorative background elements */
        .background-decor {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
            pointer-events: none;
            opacity: 0.6;
        }

        .card {
            background: rgba(255, 255, 255, 0.9);
            padding: 3rem;
            border-radius: 30px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.05);
            max-width: 500px;
            border: 2px solid #ffd1dc;
            z-index: 1;
        }

        h1 {
            font-family: 'Dancing Script', cursive;
            font-size: 3.5rem;
            color: #d63384;
            margin-bottom: 1rem;
        }

        p {
            font-size: 1.2rem;
            line-height: 1.6;
            color: #555;
            margin-bottom: 2rem;
        }

        .spotify-link {
            display: inline-block;
            background-color: #1DB954;
            color: white;
            padding: 12px 25px;
            border-radius: 50px;
            text-decoration: none;
            font-weight: bold;
            transition: transform 0.3s ease;
        }

        .spotify-link:hover {
            transform: scale(1.05);
            background-color: #1ed760;
        }

        /* Lily & Heart floating icons */
        .floating {
            position: absolute;
            font-size: 2rem;
            animation: float 6s infinite ease-in-out;
        }

        @keyframes float {
            0%, 100% { transform: translateY(0) rotate(0deg); }
            50% { transform: translateY(-20px) rotate(10deg); }
        }
    </style>
</head>
<body>

    <div class="background-decor">
        <span class="floating" style="top:10%; left:10%;">🤍</span>
        <span class="floating" style="top:20%; right:15%; animation-delay: 1s;">🌸</span>
        <span class="floating" style="bottom:15%; left:15%; animation-delay: 2s;">💖</span>
        <span class="floating" style="bottom:20%; right:10%; animation-delay: 3s;">🌸</span>
        <span class="floating" style="top:50%; left:5%; animation-delay: 1.5s;">🤍</span>
    </div>

    <div class="card">
        <h1>Happy Valentine's, Joy</h1>
        <p>
            To the person who makes every day feel a little brighter. 
            May your day be filled with as much beauty as a field of white lilies 
            and as much warmth as the songs we share. You are truly special.
        </p>
        <a href="https://open.spotify.com/track/4Y99pYp9v07k8fT9jK4D6S" target="_blank" class="spotify-link">
            Listen to Hometown Glory 🎵
        </a>
    </div>

</body>
</html>
